{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --no-audit --legacy-peer-deps && npm run build",
  "installCommand": "npm install --no-audit --legacy-peer-deps",
  "functions": {
    "api/**/*": {
      "runtime": "nodejs20.x"
    }
  }
}
